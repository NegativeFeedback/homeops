---
# Encrypt this file before committing: sops --encrypt --in-place secret.sops.yaml
apiVersion: v1
kind: Secret
metadata:
  name: headplane-config
stringData:
  config.yaml: |
    headscale:
      url: http://headscale.network.svc.cluster.local:8080
      api_key: changeme
    server:
      secret: changeme-random-32-char-string
      listen: 0.0.0.0:3000
