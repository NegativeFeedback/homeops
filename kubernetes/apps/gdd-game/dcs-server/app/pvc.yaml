---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dcs-server-config
  namespace: gdd-game
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 500Gi
