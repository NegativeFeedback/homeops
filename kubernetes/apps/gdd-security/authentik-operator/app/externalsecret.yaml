---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-operator-secret
spec:
  refreshInterval: 12h
  secretStoreRef:
    name: bitwarden
    kind: ClusterSecretStore
  data:
    - secretKey: AUTHENTIK_TOKEN
      remoteRef:
        key: "092bcce0-5420-415e-b3b7-b4aa01532019"
        property: AUTHENTIK_TOKEN
