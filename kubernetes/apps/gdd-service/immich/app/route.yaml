---
# yaml-language-server: $schema=https://kubernetes-schemas.18b.haus/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich-route
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Service
    gethomepage.dev/name: Immich
    gethomepage.dev/icon: immich.svg
    gethomepage.dev/description: "Photo & video backup"
    gethomepage.dev/siteMonitor: "https://photos.gddnet.io"
    # Templated: uncomment + fill HOMEPAGE_VAR_IMMICH_TOKEN in
    # gdd-service/homepage/app/externalsecret.yaml for a live widget.
    # gethomepage.dev/widget.type: immich
    # gethomepage.dev/widget.url: "https://photos.gddnet.io"
    # gethomepage.dev/widget.key: "{{`{{HOMEPAGE_VAR_IMMICH_TOKEN}}`}}"
    gatus.home-operations.com/enabled: "true"
    gatus.home-operations.com/endpoint: |-
      group: Media
      depends-on:
        - CFtunnel
      conditions: ["[STATUS] == 200"]
      alerts:
        - type: ntfy
        - type: discord
        - type: github
spec:
  parentRefs:
    - name: external
      namespace: kube-system
      sectionName: https
  hostnames:
    - "photos.${SECRET_DOMAIN}"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: immich-server
          port: 2283
