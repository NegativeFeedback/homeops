---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: snapotter
  namespace: database
spec:
  cluster:
    name: postgres
  name: gdd-service-snapotter
  owner: gdd-service-snapotter-user
  ensure: present
---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: snapotter-db-password
  namespace: database
spec:
  length: 48
  digits: 10
  symbols: 0
  noUpper: false
  allowRepeat: true
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: snapotter-db-secret
  namespace: database
spec:
  refreshInterval: "0" # generate password once; rotation breaks long-lived consumers (see jellystat/inventree)
  target:
    name: snapotter-db-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        # username/password keys are required by the CNPG Cluster's
        # managed.roles passwordSecret reference (see cluster.yaml) --
        # without them the role never gets created/synced.
        username: gdd-service-snapotter-user
        password: "{{ .password | toString }}"
        DATABASE_URL: "postgresql://gdd-service-snapotter-user:{{ .password | toString }}@postgres-rw.database.svc.cluster.local:5432/gdd-service-snapotter"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: snapotter-db-password
